<%- include('header') %>
    <div class="main-content">
        <div class="section__content section__content--p30">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-lg-12">

                        <div class="card-header col-md-2" style="background-color: transparent;">
                            <strong>Slider View</strong>
                        </div>

                        <form class="form-header">
                            <input class="au-input au-input--xl" type="text" name="search" placeholder="Search for datas &amp; reports..." />
                            <button class="au-btn--submit" type="submit">
                                <i class="zmdi zmdi-search"></i>
                            </button>
                        </form>

                        <div class="table-responsive table--no-card m-b-30">
                            <form method="post" action="/slider/mulDel">
                                <table class="table table-borderless table-striped table-earning first text-left ">
                                    <thead>
                                        <tr>
                                            <th>
                                                <input type="checkbox" id="multidelet">
                                                <input type="submit" value="delet">
                                            </th>
                                            <th>Title</th>
                                            <th>Description</th>
                                            <th>Image</th>
                                            <th>Status</th>
                                            <th>Operation</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% for(var sl of sliderdata) {%>
                                            <tr>
                                                <td><input type="checkbox" name="mulDel[]" class="singleDelet" value="<%= sl.id %>"></td>
                                                <td>
                                                    <%= sl.title %>
                                                </td>
                                                <td>
                                                    <%= sl.content %>
                                                </td>
                                                <td><img src="<%= sl.image %>" height="50" width="50"></td>
                                                <td>

                                                    <% if(sl.isActive) { %>
                                                        <a href="?id=<%= sl.id %>&page=<%= cpage %>&search=<%= search %>&status=deActive">Active</a>
                                                        <% } else{%>
                                                            <a href="?id=<%= sl.id %>&page=<%= cpage %>&search=<%= search %>&status=Active">Deactive</a>
                                                            <% }%>

                                                </td>
                                                <td><a href="/slider/delete/<%= sl.id %>"><i class="fa-solid fa-trash-arrow-up" style="font-size: 25px; padding-right: 20px;"></i></a>
                                                    <a href="/slider/update/<%= sl.id %>"><i class="fa-regular fa-pen-to-square" style="font-size: 25px;"></i></a></td>
                                            </tr>
                                            <% } %>

                                                <tr>
                                                    <td>
                                                        <% var pagenum=cpage %>

                                                            <% var pre=--pagenum %>
                                                                <% if(pre>0) {%>
                                                                    <a href="?page=<%= pre%>&search=<%= search %>">
                                                                    prev </a>
                                                                    <% } %>
                                                                        <a href="<%= cpage%>&search=<%= search %>">
                                                                            <%=cpage%>
                                                                        </a>
                                                                        <% var next=++cpage %>

                                                                            <% if(cpage<=pageNum){ %>
                                                                                <a href="?page=<%= next %>&search=<%= search %>">
                                                                                        next </a>
                                                                                <% } %>
                                                    </td>
                                                </tr>
                                    </tbody>
                                </table>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <%- include('footer') %>